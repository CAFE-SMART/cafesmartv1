[build]
  base = "frontend"
  command = "pnpm install --no-frozen-lockfile && pnpm build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://cafe-smart-api.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200