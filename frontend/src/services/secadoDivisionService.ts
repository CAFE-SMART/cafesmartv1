export interface DivisionCalidad { calidadOriginal: string; pesoMismaCalidad: number; pesoCalidadInferior: number; calidadInferior: string; mermaTotal: number; } export function dividirLotePorCalidad(pesoInicial: number, calidadOriginal: string, porcentajeCalidadInferior: number): DivisionCalidad { const merma = pesoInicial * (porcentajeCalidadInferior / 100); const pesoCalidadInferior = merma; const pesoMismaCalidad = pesoInicial - merma; return { calidadOriginal, pesoMismaCalidad, pesoCalidadInferior, calidadInferior: "Regular", mermaTotal: merma }; } export function getCalidadInferior(calidad: string): string { const map: Record<string, string> = { "Bueno": "Regular", "Regular": "Bajo", "Excelente": "Bueno" }; return map[calidad] || "Regular"; }
export interface DivisionCalidadResult { pesoMismaCalidad: number; pesoCalidadInferior: number; calidadInferior: string; mermaTotal: number; }
export function dividirLoteSecado(peso: number, cal: string, pct: number): DivisionCalidadResult { const merma = peso * (pct / 100); return { pesoMismaCalidad: peso - merma, pesoCalidadInferior: merma, calidadInferior: cal.includes("Bueno") ? "Regular" : "Bajo", mermaTotal: merma }; }
