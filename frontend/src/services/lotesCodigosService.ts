export function generarCodigoLote(tipo: string, calidad: string, numero: number): string { const t = tipo.substring(0,2).toUpperCase(); const c = calidad.substring(0,1).toUpperCase(); return t + "-" + c + "-" + numero.toString().padStart(2, "0"); } export function liberarCodigoLote(codigo: string): void { console.log("Liberado: " + codigo); } export function siguienteNumero(existentes: number[]): number { return existentes.length > 0 ? Math.max(...existentes) + 1 : 1; }
