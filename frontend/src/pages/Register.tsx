import React from 'react';

export default function Register() {
  return (
    <main className="page-shell">
      <section className="auth-card">
        <p className="eyebrow">Cafe Smart</p>
        <h1>Crear cuenta</h1>
        <p>Esta pantalla ya esta lista para conectar el formulario de registro.</p>
      </section>
    </main>
  );
}
